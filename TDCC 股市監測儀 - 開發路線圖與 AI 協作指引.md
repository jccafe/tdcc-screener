TDCC 股市監測儀 - 開發路線圖與 AI 協作指引
這份文件旨在指導您如何從零開始，或者與 AI 助手協作開發這套台股籌碼監控系統。

一、 環境設定與注意事項
1. 核心依賴
Python 3.10+: 建議使用 conda 或 venv 隔離環境。
FastAPI + Uvicorn: 異步後端。
SQLAlchemy + SQLite: 輕量級儲存。
yfinance: 股價數據源（注意：其並非官方 API，有頻率限制）。
Pandas: 核心數據處理引擎。
2. 重要注意事項
編碼 (Encoding): 台灣 TDCC 的 CSV 下載往往是 MS950 或 Big5，處理時需顯式指定 encoding='utf-8' (如果已轉碼) 或讀取原始 CSV 時注意編碼。
yfinance 後綴: 上市需加 .TW，上櫃需加 .TWO。
並發限制: 大量下載股價時應使用 batch_download 減少連線次數。
二、 詳細開發步驟與 AI 提示詞
階段 1：數據抓取模組 (The Crawler)
目標: 定期從 TDCC 官網下載 CSV 並解析。

提示詞:
「請幫我寫一個 Python 腳本，使用 requests 下載台灣集保結算所 (TDCC) 的股權分散表 CSV。

需要處理動態日期參數（因為 URL 包含日期）。
下載後使用 Pandas 解析，並過濾出特定等級（例如散戶層級 1-9，大戶層級 14-15）的持股比例與人數。
實作一個迴圈，可以一次抓取過去 12 週的歷史數據。」
階段 2：資料庫設計 (The Database)
目標: 建立 SQLite 數據模型，支援快速查詢特定股票的歷週變化。

提示詞:
「幫我設計一個 SQLAlchemy 模型來儲存台股集保數據。

攔位包含：日期、股票代號、持股分級、人數、持股比例。
需要支援資料覆寫（如果日期與代號已存在，則更新而非重複插入）。
寫一個 function 支援將資料庫轉為 Pivot Table，以便後續計算週與週之間的變化量。」
階段 3：篩選核心邏輯 (The Screener)
目標: 實作量化篩選與評分模型。

提示詞:
「請實作一個股市篩選器邏輯：

從資料庫讀取過去 N 週數據。
條件：大戶持股比例連續 N 週上升，且散戶人數連續 N 週下降。
計算評分：大戶增持 5% 拿 40 分，散戶減持 8% 拿 40 分，加上基礎分 20 分。
最後使用 yfinance 檢查股價，過濾出價格離 20MA 距離在 5% 以內的股票。」
階段 4：後端 API 與 ETA (The API)
目標: 暴露 API 並實作進度追蹤與剩餘時間預估。

提示詞:
「使用 FastAPI 建立後端架構：

/api/screener: 啟動背景任務開始篩選。
/api/progress: 回傳當前百分比。
重點：請在處理循環中實作 ETA 計算邏輯，根據處理過的股票數量與花費時間，計算預計剩下的秒數。」
階段 5：前端 UI/UX (The Frontend)
目標: 現代感毛玻璃介面與 Plotly 整合。

提示詞:
「請幫我用 HTML/CSS/JS 寫一個現代、具備毛玻璃效果的股市儀表板。

左側為控制面板（包含滑桿、下拉選單、日期選擇）。
右側為結果表格，評分欄位要根據數值顯示不同的顯色（高分顯示紅色、低分藍色）。
實作 Plotly K 線圖 Modal，點擊按鈕後畫出該股近一個月的股價走勢，並自動處理上市櫃 (.TW/.TWO) 補貼邏輯。」
三、 進階優化提示詞 (Optimization)
當基礎功能完成後，可使用以下提示詞優化：

快取優化: 「請幫我在 FastAPI 增加一個股票股價快取機制，使用 JSON 儲存已抓過的歷史股價，過期時間 1 小時。」
UI 優化: 「表格列太多時會造成水平捲動，請幫我縮小 Padding、調整字體大小，並讓左側固定寬度、右側主區塊自適應。」
產業對照: 「yfinance 回傳的產業名稱是英文，請寫一個映射函數對應到中文大類別，確保產業篩選器能正常運作。」
© 2026 TDCC 開發路線指引