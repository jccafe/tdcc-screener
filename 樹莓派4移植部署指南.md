# TDCC 股市監測儀 - Raspberry Pi 4 移植部署指南

## 1. 環境需求
- **Raspberry Pi 4B** (4GB+ 建議)。
- **Raspberry Pi OS (64-bit)**。

## 2. 部署步驟
1. **系統依賴**: `sudo apt install python3-pip python3-venv git libatlas-base-dev`
2. **建立虛擬環境**:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install fastapi uvicorn sqlalchemy requests pandas yfinance plotly passlib python-multipart
   ```
3. **啟動腳本 (Systemd)**:
   建立 `/etc/systemd/system/tdcc.service` 並設定 `ExecStart` 指向您的 `main.py`。

## 3. 性能優化
- 將 `stock_price_cache` 掛載至 **TMPFS** (內存) 以減少 SD 卡讀寫並提升速度。
- 設定靜態 IP 方便區網內其他裝置連線。

## 🛠️ 日常維護與快速更新

為了保持系統處於最佳狀態，建議您熟悉以下操作：

### 1. 一鍵同步 PC 端的新功能
當您在電腦端修改了程式碼並推送到 GitHub 後，在樹莓派執行以下「三部曲」即可完成更新：
```bash
cd ~/tdcc-screener
git pull
sudo systemctl restart tdcc.service
```

### 2. 即時監控系統運行狀況
如果您想查看伺服器目前的連線紀錄或是否有報錯：
```bash
sudo journalctl -u tdcc.service -f
```

### 3. 清理數據快取
如果篩選結果顯示不正確，或您想重新計算所有指標：
```bash
# 刪除計算快取 (不會刪除資料庫)
rm ~/tdcc-screener/tdcc_project2/cache/*.json
# 重啟服務以生效
sudo systemctl restart tdcc.service
```

### 4. 服務狀態控制
- **停止服務**：`sudo systemctl stop tdcc.service`
- **啟動服務**：`sudo systemctl start tdcc.service`
- **檢查狀態**：`sudo systemctl status tdcc.service`

---

© 2026 股市散戶監測儀 - 樹莓派部署指南
